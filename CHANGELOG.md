# Changelog

The changelog lives at **[`docs/CHANGELOG.md`](docs/CHANGELOG.md)**, alongside the rest of the
project memory system.

This pointer exists because a root `CHANGELOG.md` is a strong convention and people look here
first.
